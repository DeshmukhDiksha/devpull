# React + Vite
1. Create react app with Vite npm create vite@latest
2. Intall tailwindcss, postcss and autoprefixer
3. Update index.css with @import tailwindcss and update the tailwind.config for files lookup and also update vite.config 
4. Install daisyUI
5. Update tailwind css with plugins: [daisyui],  daisyui: {   themes: ["dark", "cupcake"], }.
6. Add navbar to dev pull .
